import {createHmac,timingSafeEqual} from "node:crypto";
import {createClient} from "@supabase/supabase-js";
export const runtime="nodejs";
function validSignature(body:string,header:string,secret:string){const parts=Object.fromEntries(header.split(",").map(v=>v.split("="))),timestamp=parts.t,signature=parts.v1;if(!timestamp||!signature||Math.abs(Date.now()/1000-Number(timestamp))>300)return false;const expected=createHmac("sha256",secret).update(`${timestamp}.${body}`).digest("hex");return signature.length===expected.length&&timingSafeEqual(Buffer.from(signature),Buffer.from(expected))}
export async function POST(request:Request){const secret=process.env.STRIPE_WEBHOOK_SECRET,service=process.env.SUPABASE_SERVICE_ROLE_KEY,url=process.env.NEXT_PUBLIC_SUPABASE_URL;if(!secret||!service||!url)return new Response("Not configured",{status:503});const body=await request.text(),signature=request.headers.get("stripe-signature")??"";if(!validSignature(body,signature,secret))return new Response("Invalid signature",{status:400});const event=JSON.parse(body);if(event.type==="checkout.session.completed"&&event.data.object.payment_status==="paid"){const session=event.data.object,paymentId=session.metadata?.payment_id;if(paymentId){const admin=createClient(url,service,{auth:{persistSession:false}}),{error}=await admin.rpc("complete_stall_payment",{target_payment:paymentId,provider_payment:String(session.payment_intent),provider_session:String(session.id)});if(error)return new Response(error.message,{status:500})}}return Response.json({received:true})}
