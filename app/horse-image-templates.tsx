/* eslint-disable @next/next/no-img-element */
"use client";
import{useCallback,useEffect,useMemo,useState}from"react";
import{createClient}from"@/lib/supabase/client";
import{GAME}from"@/lib/game/config";
const supabase=createClient();
type Kind="body_template"|"base_coat"|"modifier"|"pattern"|"face_marking"|"leg_marking"|"mane_tail";
type Asset={id:string;asset_key:string;asset_type:Kind;breed:string|null;sex:"Mare"|"Stallion"|null;phenotype_key:string|null;marking_key:string|null;leg_position:string|null;image_url:string;z_index:number;status:"pending"|"approved"|"rejected";active:boolean;review_notes:string};
const kinds:Kind[]=["body_template","base_coat","modifier","pattern","face_marking","leg_marking","mane_tail"],blank={asset_key:"",asset_type:"body_template"as Kind,breed:GAME.foundationBreeds[0]as string,sex:"",phenotype_key:"",marking_key:"",leg_position:"",image_url:"",z_index:0};
export function HorseImageTemplates({notify}:{notify:(s:string)=>void;changed:()=>void}){const[assets,setAssets]=useState<Asset[]>([]),[form,setForm]=useState(blank),[editing,setEditing]=useState<string|null>(null),[busy,setBusy]=useState(false),[preview,setPreview]=useState({breed:GAME.foundationBreeds[0]as string,sex:"Mare",color:"palomino",pattern:"none",face:"blaze",left_front:"sock",right_front:"none",left_hind:"stocking",right_hind:"sock"});
 const load=useCallback(async()=>{const{data,error}=await supabase.rpc("admin_list_horse_visual_assets");if(error)notify(error.message);else setAssets((data??[])as Asset[])},[notify]);useEffect(()=>{void load()},[load]);
 const upload=async(file:File)=>{if(!["image/png","image/webp"].includes(file.type)||file.size>5*1024*1024){notify("Visual layers must be transparent PNG or WebP files under 5 MB.");return}setBusy(true);const{data:user}=await supabase.auth.getUser(),path=`${user.user!.id}/visual-library/${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,"-")}`,result=await supabase.storage.from("legacy-equine-media").upload(path,file,{upsert:false});if(result.error)notify(result.error.message);else setForm(x=>({...x,image_url:supabase.storage.from("legacy-equine-media").getPublicUrl(path).data.publicUrl}));setBusy(false)};
 const save=async()=>{setBusy(true);const{error}=await supabase.rpc("admin_save_horse_visual_asset",{p_id:editing,p_asset_key:form.asset_key,p_asset_type:form.asset_type,p_breed:form.breed,p_sex:form.sex,p_phenotype_key:form.phenotype_key,p_marking_key:form.marking_key,p_leg_position:form.leg_position,p_image_url:form.image_url,p_z_index:form.z_index});notify(error?.message??"Visual asset saved as pending review.");if(!error){setEditing(null);setForm(blank);await load()}setBusy(false)};
 const review=async(asset:Asset,status:"approved"|"rejected",active:boolean)=>{const{error}=await supabase.rpc("admin_review_horse_visual_asset",{p_id:asset.id,p_status:status,p_active:active,p_notes:prompt("Review notes")??""});notify(error?.message??`Visual asset ${status}.`);if(!error)await load()};
 const layers=useMemo(()=>assets.filter(a=>a.status==="approved"&&a.active&&(a.breed==null||a.breed==="*"||a.breed===preview.breed)&&(a.sex==null||a.sex===preview.sex)&&((a.asset_type==="body_template")||(a.asset_type==="base_coat"&&a.phenotype_key===preview.color)||(a.asset_type==="modifier"&&a.phenotype_key===preview.color)||(a.asset_type==="pattern"&&a.marking_key===preview.pattern)||(a.asset_type==="face_marking"&&a.marking_key===preview.face)||(a.asset_type==="leg_marking"&&a.leg_position&&a.marking_key===preview[a.leg_position as keyof typeof preview])||(a.asset_type==="mane_tail"&&a.phenotype_key===preview.color))).sort((a,b)=>a.z_index-b.z_index),[assets,preview]);
 return <section className="panel visuallibrary"><p className="eyebrow">OWNER / ADMIN · APPROVED ASSETS ONLY</p><h2>Horse Visual Library</h2><p className="panelsub">Production horses are assembled deterministically from reviewed breed templates and genetic phenotype layers. This library never changes horse genetics.</p><div className="adminformgrid"><label>Asset key<input placeholder="morgan_mare_02" value={form.asset_key} onChange={e=>setForm({...form,asset_key:e.target.value})}/></label><label>Layer type<select value={form.asset_type} onChange={e=>setForm({...form,asset_type:e.target.value as Kind})}>{kinds.map(x=><option key={x} value={x}>{x.replaceAll("_"," ")}</option>)}</select></label><label>Breed<select value={form.breed} onChange={e=>setForm({...form,breed:e.target.value})}><option value="*">Generic fallback</option>{GAME.foundationBreeds.map(x=><option key={x}>{x}</option>)}</select></label><label>Sex<select value={form.sex} onChange={e=>setForm({...form,sex:e.target.value})}><option value="">Any</option><option>Mare</option><option>Stallion</option></select></label><label>Phenotype key<input placeholder="palomino" value={form.phenotype_key} onChange={e=>setForm({...form,phenotype_key:e.target.value.toLowerCase()})}/></label><label>Marking key<input placeholder="blaze / sock / tobiano" value={form.marking_key} onChange={e=>setForm({...form,marking_key:e.target.value.toLowerCase()})}/></label><label>Leg position<select value={form.leg_position} onChange={e=>setForm({...form,leg_position:e.target.value})}><option value="">Not a leg layer</option>{["left_front","right_front","left_hind","right_hind"].map(x=><option key={x}>{x}</option>)}</select></label><label>Layer order<input type="number" value={form.z_index} onChange={e=>setForm({...form,z_index:Number(e.target.value)})}/></label><label>Transparent asset<input type="file" accept="image/png,image/webp" onChange={e=>e.target.files?.[0]&&void upload(e.target.files[0])}/></label></div><button className="primary" disabled={busy||!form.asset_key.trim()||!form.image_url} onClick={save}>{editing?"Save Replacement for Review":"Add Pending Asset"}</button><h3>Preview Horse Combination</h3><div className="visualpreviewcontrols"><select value={preview.breed} onChange={e=>setPreview({...preview,breed:e.target.value})}>{GAME.foundationBreeds.map(x=><option key={x}>{x}</option>)}</select><select value={preview.sex} onChange={e=>setPreview({...preview,sex:e.target.value})}><option>Mare</option><option>Stallion</option></select><input aria-label="Preview color" value={preview.color} onChange={e=>setPreview({...preview,color:e.target.value.toLowerCase()})}/><input aria-label="Preview pattern" value={preview.pattern} onChange={e=>setPreview({...preview,pattern:e.target.value.toLowerCase()})}/><input aria-label="Preview face" value={preview.face} onChange={e=>setPreview({...preview,face:e.target.value.toLowerCase()})}/></div><div className="layerpreview">{layers.map(a=><img key={a.id} src={a.image_url} alt="" style={{zIndex:a.z_index}}/>)}{!layers.length&&<img src="/foundation-horse.png" alt="Official Foundation fallback"/>}</div><small>{layers.length?layers.map(x=>x.asset_key).join(" → "):"Incomplete combination — official Foundation fallback"}</small><div className="templategrid">{assets.map(a=><article key={a.id}><img src={a.image_url} alt={a.asset_key}/><p className="eyebrow">{a.asset_type} · {a.status}{a.active?" · ACTIVE":""}</p><h3>{a.asset_key}</h3><p>{a.breed??"Any breed"} · {a.sex??"Any sex"}</p><div className="buttonrow"><button onClick={()=>{setEditing(a.id);setForm({asset_key:a.asset_key,asset_type:a.asset_type,breed:a.breed??"*",sex:a.sex??"",phenotype_key:a.phenotype_key??"",marking_key:a.marking_key??"",leg_position:a.leg_position??"",image_url:a.image_url,z_index:a.z_index})}}>Replace</button>{a.status!=="approved"&&<button onClick={()=>void review(a,"approved",true)}>Approve & Activate</button>}{a.status==="approved"&&<button onClick={()=>void review(a,"approved",!a.active)}>{a.active?"Deactivate":"Activate"}</button>}<button onClick={()=>void review(a,"rejected",false)}>Reject</button></div></article>)}</div></section>}
