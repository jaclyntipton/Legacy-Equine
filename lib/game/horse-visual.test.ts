import {describe,expect,it} from "vitest";
import {buildHorseImagePrompt,type VisualPhenotype} from "./horse-visual";
const visual:VisualPhenotype={breed:"Quarter Horse",sex:"Mare",age_years:2,color:"Palomino",body:"compact stock-horse build",height_range:"typically 14.2–16 hands",movement:"quick balanced movement",temperament:"steady and willing",breed_constraints:"avoid exaggerated halter musculature",coat:"rich golden cream",mane_tail:"light cream",pattern:"solid",face_marking:"blaze",left_front:"sock",right_front:"none",left_hind:"stocking",right_hind:"sock",pose:"standing square",view:"side profile",background:"neutral studio"};
describe("horse visual prompt",()=>{it("uses calculated phenotype and persistent markings without raw genetics",()=>{const prompt=buildHorseImagePrompt(visual);expect(prompt).toContain("Palomino");expect(prompt).toContain("left front sock");expect(prompt).toContain("right front none");expect(prompt).toContain("compact stock-horse build");expect(prompt).toContain("avoid exaggerated halter musculature");expect(prompt).toContain("recognizable as a Quarter Horse from silhouette");expect(prompt).toContain("do not produce a generic all-purpose AI horse");expect(prompt).not.toContain("Extension");});});
