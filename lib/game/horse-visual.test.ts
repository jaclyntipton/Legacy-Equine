import {describe,expect,it} from "vitest";
import {buildHorseImagePrompt,type VisualPhenotype} from "./horse-visual";
const visual:VisualPhenotype={breed:"Quarter Horse",sex:"Mare",age_years:2,color:"Palomino",body:"compact stock-horse build",coat:"rich golden cream",mane_tail:"light cream",pattern:"solid",face_marking:"blaze",left_front:"sock",right_front:"none",left_hind:"stocking",right_hind:"sock",pose:"standing square",view:"side profile",background:"neutral studio"};
describe("horse visual prompt",()=>{it("uses calculated phenotype and persistent markings without raw genetics",()=>{const prompt=buildHorseImagePrompt(visual);expect(prompt).toContain("Palomino");expect(prompt).toContain("left front sock");expect(prompt).toContain("right front none");expect(prompt).toContain("compact stock-horse build");expect(prompt).not.toContain("Extension");});});
