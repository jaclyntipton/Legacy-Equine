export function safeShowEntryError(message?:string){const value=(message??"").toLowerCase();if(value.includes("already entered")||value.includes("duplicate"))return "That horse is already entered in this show.";if(value.includes("tier")||value.includes("not eligible"))return "This horse is not eligible for this competition tier.";if(value.includes("insufficient")||value.includes("not enough"))return "You don't have enough LE Dollars to enter this horse.";if(value.includes("no longer open")||value.includes("show is full")||value.includes("entries remain"))return "This show is no longer accepting those entries.";if(value.includes("owned by")||value.includes("choose a horse you own"))return "That horse is no longer available to enter from your stable.";return "We couldn't complete that show entry. Please try again."}
