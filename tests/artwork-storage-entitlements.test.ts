import{describe,expect,it}from"vitest";import{readFileSync}from"node:fs";const sql=readFileSync("supabase/migrations/202609160019_artwork_album_entitlements.sql","utf8"),ui=readFileSync("app/artwork-storage-admin.tsx","utf8");describe("Artwork Album storage entitlements",()=>{it("gives Owner #1 a real unlimited capability",()=>{expect(sql).toContain("unlim:=s.account_number=1 or");expect(sql).toContain("'effective_capacity',case when unlim then null");expect(sql).not.toContain("999999")});it("combines configurable subscriber base and owner bonus",()=>{expect(sql).toContain("subscriber_base_capacity integer not null default 100");expect(sql).toContain("base+bonus");expect(sql).toContain("subscription_dependent")});it("preserves existing artwork when capacity drops",()=>{expect(sql).not.toMatch(/delete from artwork_album_items/);expect(sql).toContain("'over_capacity'");expect(sql).toContain("Artwork Album is at capacity")});it("enforces upload restrictions server-side",()=>{expect(sql).toContain("5242880");expect(sql).toContain("1200");expect(sql).toContain("image/jpeg");expect(sql).toContain("finalize_artwork_album_upload")});it("provides audited Owner gift controls",()=>{expect(ui).toContain("[50,100,250,500]");expect(ui).toContain("Grant Unlimited Artwork Storage");expect(ui).toContain("Remove / Change Bonus");expect(sql).toContain("artwork_storage_gift_history");expect(sql).toContain("admin_audit_log")})});
