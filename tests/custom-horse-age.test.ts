import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const sql=readFileSync(new URL("../supabase/migrations/202609170017_fix_custom_horse_age_initialization.sql",import.meta.url),"utf8");describe("custom horse authoritative age",()=>{it("uses the configured 28-real-day horse year for creation",()=>{expect(sql).toContain("horse_birth_date_for_age(horse_age_years,anchor)");expect(sql).not.toContain("horse_age_years*365.25/30")});it("uses authoritative age in Horse Editor data",()=>{expect(sql).toContain("'calculated_age',horse_game_age(h.birth_date)")});it("routes direct editor age changes through the audited age RPC",()=>{expect(sql).toContain("perform owner_set_horse_age");expect(sql).toContain("'age_change'")});it("repairs only provenance-proven Owner Unicorns from their original creation anchor",()=>{expect(sql).toContain("lower(x.breed)='unicorn'");expect(sql).toContain("horse_birth_date_for_age(3,h.created_at)");expect(sql).toContain("corrected<>2")})});
