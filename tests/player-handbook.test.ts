import{describe,it,expect}from"vitest";import{readFileSync}from"node:fs";
const sql=readFileSync(new URL("../supabase/migrations/202609200002_player_handbook.sql",import.meta.url),"utf8"),ui=readFileSync(new URL("../app/handbook.tsx",import.meta.url),"utf8"),admin=readFileSync(new URL("../app/admin-handbook.tsx",import.meta.url),"utf8"),shell=readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8");
describe("Player Handbook",()=>{it("seeds the requested structured categories",()=>{for(const name of["Getting Started","Your Stable","Horses","Horse Development & Stats","Feeding","Tack & Equipment","Training","Showing","Breeding","Marketplace","Professions","World & Economy","Community","Account Levels & Progression","FAQ"])expect(sql).toContain(`'${name}'`)});it("keeps searchable published articles server-authoritative",()=>{expect(sql).toContain("get_handbook(p_search");expect(sql).toContain("handbook_game_config");expect(sql).toContain("show_tiers");expect(sql).toContain("show_ap_config");expect(sql).toContain("feed_system_config")});it("makes AP horse-specific and excludes QA instructions",()=>{expect(sql).toContain("AP belongs exclusively to the individual horse");expect(sql).not.toMatch(/Owner QA controls|QA bypass/)});it("provides safe Owner publishing controls",()=>{expect(sql).toContain("regexp_replace");expect(sql).toContain("admin_save_handbook_article");expect(admin).toContain("published");expect(admin).toContain("archived")});it("is linked from account navigation and onboarding",()=>{expect(shell).toContain("How to Play before you begin");expect(shell).toContain('navigateHref("/how-to-play")');expect(ui).toContain("Search the Player Handbook")})});
