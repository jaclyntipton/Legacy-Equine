import{describe,it,expect}from"vitest";import{readFileSync}from"node:fs";
const sql=readFileSync("supabase/migrations/202609180020_profession_business_storefronts.sql","utf8"),ui=readFileSync("app/profession-businesses.tsx","utf8"),professions=readFileSync("app/professions.tsx","utf8"),player=readFileSync("app/community-directory.tsx","utf8"),stable=readFileSync("app/public-stable-profile.tsx","utf8");
describe("profession business storefronts",()=>{it("maps all five professions and leaves Nutritionist future-ready",()=>{for(const value of["Blacksmith Shop","Veterinary Clinic","Training Facility","Equine Therapy Room","Tack Shop"])expect(sql).toContain(value);expect(sql).not.toContain("when'nutritionist'")});it("unlocks businesses only from legitimate certificates",()=>{expect(sql).toContain("player_profession_certifications")});it("uses existing offering and rate architecture",()=>{expect(ui).toContain('rpc("set_service_offering"');expect(sql).toContain("player_service_offerings");expect(sql).toContain("service_catalog")});it("provides dedicated management and public storefront routes",()=>{expect(professions).toContain("/professions/businesses");expect(professions).toContain("PublicBusiness");for(const label of["Business Profile","Services","Pricing","History","Create Tack","Tack Shop Inventory"])expect(ui).toContain(label)});it("links Profile, Stable, and market to storefronts",()=>{expect(player).toContain("/professions/storefront/");expect(stable).toContain("/professions/storefront/");expect(professions).toContain("/professions/storefront/${provider.provider_id}")});it("excludes QA-only data from public projections",()=>{expect(sql).toContain("player_profession_certifications");expect(sql).toContain("o.enabled");expect(sql).toContain("o.price between sc.min_price and sc.max_price");expect(sql).not.toContain("Owner QA")});it("sanitizes content and rejects executable banner values",()=>{expect(sql).toContain("business_safe_text");expect(sql).toContain("regexp_replace");expect(sql).toContain("Invalid business banner URL")})});
